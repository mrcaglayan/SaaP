STEP-FA01 accepted with known follow-ups: existing source-type literal consumers remain intentionally unmigrated for now; secondary RBAC helper currently asserts permission presence without caller-supplied scope matching and may need scoped extension when FA06/FA11 consumers land.   
STEP-FA02 accepted with follow-ups: preflight surface and normalization rule are in place, zero-PRIMARY is warning-only, but final smoke coverage still needs a non-empty seeded/fixture run, and FA03 file scope must be reconciled because the detected duplicate-PRIMARY writer path currently points to cari.settlement.service.js.
STEP-FA03 accepted. All audited in-scope CARI/cash/payments writer flows now appear compliant with the one-owning-PRIMARY rule, posting/reversal smoke passed, and duplicate-PRIMARY preflight is clean. One justified variance was required: cash.exchange.service.js was modified because it was the actual in-scope cash writer emitting the duplicate PRIMARY behavior, even though it was not listed in the original allowed-files block.
STEP-FA04 accepted. Inventory and payroll writer flows were audited and found compliant with the one-owning-PRIMARY rule, preflight remained clean, and release-gate smokes passed. No code changes were required. One non-blocking note: payroll.corrections.service.js was referenced during audit but is outside the literal FA04 allowed-files list, so treat that as an audit observation rather than an in-scope file target.
STEP-FA05 accepted. The repo now has meaningful regression backing for the one-owning-PRIMARY contract: static guardrails, runtime posting-flow coverage for known cleaned-up producers, and post-suite preflight verification. The release-gate exits non-zero on violation, and the earlier out-of-scope tracker edit was reverted.
STEP-FA06 accepted. Backend now owns the drillback/reverse-block destination contract additively: legacy reverse messages remain, details.reverseBlock is available for blocked reversals, single-journal detail reads expose source_links[].destination and reverseBlock without replacing raw source_links, and the bulk journals list path remains unchanged. 
STEP-FA07 accepted. Journal Workbench now prefers backend-owned destination metadata for both normal drillback and reverse-block handling, while preserving fallback-local behavior for non-upgraded source types. The duplicated local routing model has been consolidated into a shared frontend helper, and no fixed-assets-specific hardcoded route map remains required for upgraded paths.
STEP-FA08 accepted. The OpenAPI generator now recognizes fixed-assets as a first-class surface through FixedAssets tag support and /api/v1/fixed-assets path inference, openapi generation/checks remain healthy, and the absence of FixedAssets-tagged paths in the generated spec is expected until later backend fixed-assets routes are added.
STEP-FA09 accepted. The m138 foundation migration appears to follow the locked dependency-safe base-table order, uses staged DDL for the minimum forward/circular intra-m138 references, and correctly defers custodian foreign keys plus the broader constraint/index pass to later work.
STEP-FA10 accepted. The m138 migration now appears to contain the intended constraint/index hardening, uses MySQL-realistic DB-backed uniqueness for DRAFT runs and current-effective posted depreciation, and correctly leaves custodian FKs to FA11 and traceability/journal_source_links hardening to FA12.
STEP-FA11 accepted. The interim custodian table now exists, the three deferred custodian foreign keys are attached only after that table is present, and the work stayed correctly limited to the schema side of the custodian surface without pulling in later UI/workflow scope.
STEP-FA12 accepted. The m140 migration now appears to complete the fixed-assets schema tail by adding the remaining CARI traceability support and DB-backed one-PRIMARY-per-journal enforcement using a MySQL-realistic generated-column strategy, while preserving nullable provenance fields for manual/internal workflows and avoiding any new CARI_DOCUMENT_LINE source type.
STEP-FA13 accepted. The backend fixed-assets module now exists in stable owning files, /api/v1/fixed-assets is mounted, shared scope resolution has a dedicated owner, and route ordering is safely structured so later static and nested-prefixed feature slices can land without being swallowed by /:assetId.
STEP-FA14 accepted. Backend fixed-assets category CRUD is live for list/create/update, with validated threshold/salvage/account defaults, code uniqueness within (tenant_id, legal_entity_id), uppercase ACTIVE/INACTIVE status handling, legal-entity account ownership checks, expected account_type enforcement, and locked salvage-rule types NONE/FIXED_BASE_AMOUNT/PERCENT_OF_COST.
STEP-FA15 accepted. Backend fixed-assets depreciation-profile CRUD is live for list/create/update, with code uniqueness within (tenant_id, legal_entity_id), uppercase status handling, method validation to STRAIGHT_LINE/DECLINING_BALANCE/NONE, method-rate compatibility enforcement, and switchToStraightLine persisted as the locked permanent-switch flag.
STEP-FA16 accepted. Custodians are now list/create/update capable, frontend settings pages are API-backed for categories/profiles/custodians, API helpers cover all three settings surfaces, and page-level permission gating is enforced for fixed_assets.settings.* and fixed_assets.custodian.*.
STEP-FA17 accepted. Backend asset-register list/read contract is live on GET /api/v1/fixed-assets with the full locked filter set, separate owner-OU and location-OU semantics, nullable department/cost-center code filtering, lifecycle-based disposed filtering, and a stable register row shape for later frontend list work.
STEP-FA18 accepted. Backend asset-detail read is live on GET /api/v1/fixed-assets/:assetId with a real detail-hub payload covering overview/accounting/schedule-transactions-evidence-audit foundations, explicit lineage plus snapped runtime depreciation/salvage fields, and distinct owner-OU/location-OU/custodian exposure.
STEP-FA19 accepted. Fixed-assets list/detail frontend pages are now real and API-backed, consuming the FA17/FA18 backend read contracts with the full locked register filter set, explicit tab foundations on the detail page, visible profile-lineage versus frozen-runtime separation, and clear owner-OU/location-OU/custodian distinction throughout the UI.
STEP-FA20 accepted. Manual asset drafts can now be created and DRAFT-updated with backend-owned transaction-safe FA-###### numbering, category/profile default prefill, snapped profile fields, resolved salvage values, and strict DRAFT-only mutability boundaries.
STEP-FA21 accepted. Standard manual activation is fully green with live API/DB smoke coverage: valid activation reaches ACTIVE and creates exactly one ACQUISITION with zero CAPITALIZATION rows, invalid activation cases reject cleanly without creating transactions, and post-activation draft PATCH is blocked by backend-enforced freeze semantics.
STEP-FA22 accepted. Manual legacy-onboarding activation is now fully green with reusable backend smoke coverage on tenant 1: valid below-threshold legacy imports with remaining depreciation activate to ACTIVE with exactly one non-posting ACQUISITION row, zero-remaining imports activate to FULLY_DEPRECIATED with no low-value auto-expense, invalid legacy NBV math rejects cleanly, and the previously missing source-linked/CARI rejection branch is now live-smoke verified.
Carry-forward from STEP-FA23: re-check low-value asset exclusion logic at STEP-FA30 schedule-generation eligibility and confirm again at STEP-FA31 run preview/draft creation, because exclusion must rely on runtime state (e.g. FULLY_DEPRECIATED / remaining life / depreciation_method='NONE') rather than raw profile presence alone.
STEP-FA24 accepted. FA06 eligible AP-line selection is now driven by real remaining-unit math with reusable smoke coverage for untouched lines, draft-linked unit consumption and release, invalid quantity ineligibility, oversized unitCount rejection, and enforced cari.doc.read permission gating.
STEP-FA25 accepted. Same-OU FA06 capitalization is live on POST /api/v1/fixed-assets/from-cari-document-line with deterministic lowest-slot unit allocation, exact line/unit provenance, CAPITALIZATION semantics distinct from manual ACQUISITION, shared journal-link ownership, and correct low-value split handling through inline LOW_VALUE_FULL_EXPENSE depreciation when per-unit amounts fall below threshold.
STEP-FA26 accepted. Cross-OU FA06 capitalization now posts through the locked direct due-to/due-from template with directional self-balancing resolution from source document OU to asset owner OU, blocks cleanly before persistence when setup is missing, and leaves the FA25 same-OU path unchanged.
STEP-FA27 accepted. FA06 source-linked draft assets now revalidate current CARI source state at activation time, auto-refresh only source-derived values, hard-block invalid reserved-slot/source-state/threshold-path drift, and preserve all user-owned setup fields without silent reassignment.
STEP-FA28 accepted. Backend depreciation schedule read is fully green on GET /api/v1/fixed-assets/:assetId/depreciation-schedule with month-aligned non-adjustment fiscal-period resolution, locked YYYY-MM period keys, deterministic STRAIGHT_LINE and NONE schedule math, salvage-floor enforcement, and schedule generation based only on frozen activated-asset fields rather than mutable profile/category rows.
STEP-FA29 accepted. The schedule engine is fully green for DECLINING_BALANCE, permanent switch_to_straight_line behavior, salvage-floor enforcement, and forward-only legacy-onboarding schedules that start from imported NBV and stored remaining useful life without historical backfill.
Carry-forward note for FA30: lifecycle proration now correctly depends on explicit posted SUSPEND / REACTIVATE history rows. This is aligned with the plan. However, any legacy or out-of-band assets that were only moved to SUSPENDED at the master-status level without a persisted lifecycle transaction will need remediation or backfill outside FA30, because schedule cutoff dates cannot be inferred safely from status alone.
FA31 carry-forward resolution:
- Run detail/delete/post/reverse are resolved by FA32, FA33, and FA34 respectively.
- Row-by-row persistence hardening is explicitly resolved by FA35 chunked persistence.
- Run-header posting_date persistence is not clearly owned by a later step in the visible Plan 38 text; later steps validate and use postingDate, but do not explicitly add a posting_date column on the run header.
STEP-FA32 accepted with carry-forward note. Depreciation run list/detail read and DRAFT-only delete are live, frozen run headers/lines/allocations are exposed without recomputation, delete safely removes the persisted snapshot in one transaction, and the same scope can create a fresh DRAFT run again after delete.