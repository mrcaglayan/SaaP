STEP-SL01 is green. The migration-only schema foundation is in place with persisted fixed_asset_mode, expanded-line metadata, pre_disposal_status, the required FKs/CHECKs/indexes, and verified fresh/existing DB compatibility without touching existing rows.
STEP-SL02 is green. The validator now matches the exact bounded contract: backward-compatible subledger inference is intact, itemCardId still does not imply STOCK, AP fixed-asset mode rules are enforced, and AR FIXED_ASSET lines normalize internally to LINK_EXISTING for SL01/SL03 persistence compatibility.
STEP-SL03 is green. The earlier yellow blocker was real and useful: live DB smoke exposed a replaceDocumentLinesTx placeholder mismatch, it was fixed, and the rerun now proves the required AP/AR FIXED_ASSET persistence, target validation, NONE-line backward compatibility, and metadata round-trip behavior.
STEP-SL04 is green. AP FIXED_ASSET lines now auto-resolve from current category asset accounts, AR FIXED_ASSET lines require a manual sale proceeds account, AP manual account override is rejected, NULL default asset accounts fail early, and posting re-resolves from current state instead of trusting stale draft values.
STEP-SL05 is green. The last missing runtime guard is now proven live: LINK_EXISTING posting correctly fails when the target asset is no longer DRAFT, with no journal posting and no FA transaction side effects. Shared-journal AUTO_CREATE and LINK_EXISTING happy paths were already proven.
STEP-SL06 is green. The remaining yellow items were verification gaps only, and they are now closed: DRAFT/DISPOSED/wrong-tenant/wrong-legal-entity targets reject safely with no partial side effects, and gain routing is proven to use asset/category disposal accounts rather than the AR line’s manual proceeds account.
STEP-SL07 is green. The last missing proof is now closed: AR reversal correctly blocks when the disposed asset has subsequent transactions, and it does so safely with no reversal document, no journal reversal, and no partial FA side effects.
STEP-SL08 is green. Activation now correctly splits between SL05 CARI-capitalized assets and FA06/manual assets: CARI assets activate without a second ACQUISITION, residual-aware AUTO_CREATE slot validation works, FA06/manual activation stays unchanged, and hasCariCapitalization is exposed on asset detail.
STEP-SL09 been deleted.
STEP-SL10 is green. The selector-and-state-transition logic matches the plan, NONE remains backward-compatible, and the labels are already localized through the repo’s existing inline bilingual l(...) pattern, so not touching messages.js is not a blocker.
STEP-SL11 is green. The last missing browser proofs are now closed: the 500-line expansion guard disables correctly with the required inline warning, and the AP LINK_EXISTING + New Asset modal creates a draft asset and auto-selects it on the line without navigation away.
STEP-SL12 is green. Frontend validation now blocks invalid NONE/STOCK/FIXED_ASSET submissions with inline errors, preserves NONE backward compatibility, enforces AP-vs-AR fixed-asset rules correctly, and rechecks the 500-line cap at submit time.
STEP-SL13 is green. The acquisitions page now clearly reflects the new CARI-integrated asset purchase flow with Linked from CARI visibility, source-document drillback, preferred-flow guidance, and preserved legacy Capitalize from AP fallback.
STEP-SL14 is green. The FA detail page now launches a real CARI AR FIXED_ASSET sale draft prefilled for the current asset, and the legacy staged sale flow remains available as a usable fallback entry.
STEP-SL15 is green. The immediate-cash settlement schema migration now passes on both fresh and existing MySQL databases, and the required columns, defaults, FK constraints, and cash-register-required check are all verified.
STEP-SL16 is green. CARI posting now keeps IMMEDIATE_CASH inside one shared transaction by using tx-aware cash-creation and settlement-apply internals, AP/AR immediate-cash documents auto-create and post linked cash transactions then settle their own newly inserted open item, ACCRUAL remains unchanged, and runtime proof now also covers wrong-tenant register rejection, missing-session rejection, same-legal-entity register rejection, and full rollback when the immediate-cash branch fails after posting work has already started. STEP-SL16 is green. Immediate cash posting now settles AP/AR documents atomically inside the posting transaction, rejects wrong tenant/legal entity/session cases correctly, preserves ACCRUAL behavior, and persists the auto-linked settlement and cash IDs for reversal lookup.
STEP-SL17 is green. The CARI form now exposes accrual vs immediate-cash payment mode in the header, adapts AP/AR cash labels correctly, shows the cash-register picker only when needed, and sends the settlement header fields in the payload without changing default accrual behavior.
STEP-SL18 is green. Immediate-cash document reversal now uses the coordinated internal pair-reversal path, links reversing cash rows back to the originals, blocks partial-clear cases correctly, and preserves atomic rollback on failure.
STEP-SL19 is green. The old mixed "Cari Islemler" sidebar bucket is now split into separate Satinalma and Satis sections, audit moved to Settings/Admin, reports are duplicated AP/AR-style, and existing cari.* permission gating was preserved.
