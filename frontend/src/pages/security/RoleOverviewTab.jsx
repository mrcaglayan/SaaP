import { SelectedRoleOverviewPanel } from "./RolesPermissionsPanels.jsx";

/**
 * Renders the overview tab inside the dedicated role detail flow.
 */
export default function RoleOverviewTab(props) {
  return <SelectedRoleOverviewPanel {...props} />;
}
